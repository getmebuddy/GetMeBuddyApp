const initialState = {
  matches: [],
  potentialMatches: [],
  loading: false,
  error: null
};

const matchReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default matchReducer;
